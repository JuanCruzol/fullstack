sequenceDiagram
    participant browser
    participant server

    Note right of browser: The user writes a note and clicks Save
    Note right of browser: The JS code prevents the default submit action and adds the new note to the notes list on the page
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa (data as JSON)
    activate server
    Note left of server: The server saves the note to the notes array
    server-->>browser: 201 Created
    deactivate server